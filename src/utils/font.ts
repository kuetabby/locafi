import { VT323, Lato } from "next/font/google";

export const vt323 = VT323({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
});

export const lato = Lato({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
  preload: true,
});
